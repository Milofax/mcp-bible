version: '3.8'

services:
  mcp-bible:
    image: ghcr.io/geosp/mcp-bible:latest
    ports:
      - "3000:3000"
    environment:
      - MCP_ONLY=true
    command: ["mcp-bible"]
    restart: unless-stopped